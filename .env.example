# Environment Variables Template for PoliSim
# Phase 5.3: Deployment Infrastructure
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to git (already in .gitignore)

# ==================== DATABASE ====================
POSTGRES_DB=polisim
POSTGRES_USER=polisim
POSTGRES_PASSWORD=change_me_in_production

# Database URL (constructed from above or override directly)
DATABASE_URL=postgresql://polisim:change_me_in_production@postgres:5432/polisim

# ==================== REDIS ====================
REDIS_PASSWORD=change_me_in_production
REDIS_URL=redis://:change_me_in_production@redis:6379/0

# ==================== JWT AUTHENTICATION ====================
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=generate_random_secret_here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ==================== API CONFIGURATION ====================
API_RATE_LIMIT=1000
CORS_ORIGINS=http://localhost:8501,http://localhost:3000,https://yourdomain.com

# ==================== ENVIRONMENT ====================
POLISIM_ENV=production
FLASK_ENV=production

# ==================== EMAIL NOTIFICATIONS (Optional) ====================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
ADMIN_EMAIL=admin@yourdomain.com

# ==================== CLOUD STORAGE (Optional) ====================
# AWS S3 for policy document storage
AWS_ACCESS_KEY_ID=your_aws_key
AWS_SECRET_ACCESS_KEY=your_aws_secret
AWS_S3_BUCKET=polisim-policies
AWS_REGION=us-east-1

# ==================== MONITORING (Optional) ====================
# Sentry for error tracking
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Prometheus metrics
ENABLE_METRICS=true
METRICS_PORT=9090

# ==================== OAUTH (Optional) ====================
# GitHub OAuth
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# ==================== SSL/TLS (Production) ====================
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem
